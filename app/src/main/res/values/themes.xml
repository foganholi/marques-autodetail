<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.MarquesAutoDetail" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="android:textColor">@android:color/black</item>
        <item name="android:textColorHint">#808080</item>
        <item name="android:editTextStyle">@style/Widget.AppCompat.EditText</item>
    </style>

    <style name="Theme.MarquesAutoDetail" parent="Base.Theme.MarquesAutoDetail" />

    <style name="MadInput" parent="Widget.AppCompat.EditText">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:minHeight">52dp</item>
        <item name="android:background">@drawable/bg_input_dark</item>
        <item name="android:padding">14dp</item>
        <item name="android:textColor">@color/mad_text</item>
        <item name="android:textColorHint">@color/mad_text_muted</item>
    </style>
</resources>